const binary = async (numbers,target) => {
    let low = numbers[0];
    let high = numbers.length - 1;
    let found = false;

    while(low <= high)  {
        result = result * number;
        number = number - 1;
    }

    return result;
}

export default factorial